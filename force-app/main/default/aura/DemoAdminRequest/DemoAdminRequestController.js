({
    doInit : function(component, event) {

      
               var sPageURL = window.location.search;
       // alert("sPageURL"+sPageURL);
               var recordId = component.get("v.recordId");
        	// alert('recordId ::'+recordId);
                
              //  alert(component.get("v.pageReference").state.SOID);
       			
        }
                           
        })